version: '3'

services:
  app:
    build:
      context: .
      dockerfile: ./production_Dockerfile
    command: uwsgi --http :8000 --module nosta.wsgi 
    container_name: nosta_app_sv
    depends_on:
      - db
    expose:
      - "8000"
    networks:
      - prasic
      - nosta
    environment:
      - DJANGO_SETTINGS_MODULE=nosta.settings.production
    volumes:
      - .:/usr/src/app/
    stdin_open: true
    tty: true
    labels:
      - traefik.enable=true
      - traefik.http.routers.nosta_app_sv.rule=Host(`nosta.${DOMAIN}`)
      - traefik.http.routers.nosta_app_sv.entrypoints=https
      - traefik.http.routers.nosta_app_sv.tls=true
      - traefik.http.services.nosta_app_sv.loadbalancer.server.port=8000
      - traefik.http.routers.nosta_app_sv.tls.certresolver=${CERT_RESOLVER}

  db:
    image: mariadb:10.6.4
    restart: always
    container_name: nosta_db_sv
    networks:
      - nosta
    environment:
      MARIADB_ROOT_PASSWORD: nosta_db_root_password
    volumes:
      - ./mysql:/var/lib/mysql

networks:
  prasic:
    external: true
  nosta:
    external: true
